/* Generated by Yosys 0.9 (git sha1 1979e0b) */

module all_gates(and_a, and_b, and_y, nand_a, nand_b, nand_y, not_a, not_y, or_a, or_b, or_y, xor_a, xor_b, xor_y);
  input and_a;
  input and_b;
  output and_y;
  input nand_a;
  input nand_b;
  output nand_y;
  input not_a;
  output not_y;
  input or_a;
  input or_b;
  output or_y;
  input xor_a;
  input xor_b;
  output xor_y;
  and_gate and_instance (
    .a(and_a),
    .b(and_b),
    .y(and_y)
  );
  nand_gate nand_instance (
    .a(nand_a),
    .b(nand_b),
    .y(nand_y)
  );
  not_gate not_instance (
    .a(not_a),
    .y(not_y)
  );
  or_gate or_instance (
    .a(or_a),
    .b(or_b),
    .y(or_y)
  );
  xor_gate xor_instance (
    .a(xor_a),
    .b(xor_b),
    .y(xor_y)
  );
endmodule

module and_gate(a, b, y);
  input a;
  input b;
  output y;
  assign y = a & b;
endmodule

module nand_gate(a, b, y);
  wire _0_;
  input a;
  input b;
  output y;
  assign y = ~_0_;
  assign _0_ = a & b;
endmodule

module not_gate(a, y);
  input a;
  output y;
  assign y = ~a;
endmodule

module or_gate(a, b, y);
  input a;
  input b;
  output y;
  assign y = a | b;
endmodule

module xor_gate(a, b, y);
  input a;
  input b;
  output y;
  assign y = a ^ b;
endmodule
