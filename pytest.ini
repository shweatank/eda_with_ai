[pytest]
testpaths = tests
# our test files use plain functions; this stops pytest from trying to
# collect Pydantic models named TestResult / TestStatus as test classes
python_classes = *TestCase
addopts = -q
